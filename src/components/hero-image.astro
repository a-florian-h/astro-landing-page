---
import { Image } from "astro:assets";
import room from "~/assets/home page - Screen 1.svg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={room}
  widths={widths}
  sizes={sizes}
  alt="Classy room with a flower vase"
/>
