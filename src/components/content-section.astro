---
export interface Props {
  id: string;
  title: string;
  bg: string;
}

const { title, id, bg } = Astro.props;
---

<section
  id={id}
  class=`colors.pakistan_green:500 flex scroll-mt-24 flex-col items-center gap-4 space-y-8 ${bg}`
>
  <div class="flex flex-col items-center gap-4">
    <slot name="eyebrow" />
    <h2
      class="gradient-text text-center font-extrabold tracking-tight text-6xl"
    >
      {title}
    </h2>
  </div>
  <p class="tiger’s_eye max-w-xl text-center font-extrabold text-2xl">
    <slot name="lead" />
  </p>
  <slot />
</section>
